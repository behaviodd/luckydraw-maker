-- ============================================================
-- lucky_draws 테이블에 뽑기 횟수 버튼 옵션 컬럼 추가
-- 날짜: 2026-03-08
-- 목적:
--   운영자가 현장에서 손님에게 부여할 뽑기 횟수 버튼 목록 설정
--   예: [1, 2, 3, 5, 10] → 현장 화면에 해당 숫자 버튼 표시
--   기존 드로우는 기본값 [1,2,3,5,10] 자동 적용
-- ============================================================

ALTER TABLE lucky_draws
ADD COLUMN IF NOT EXISTS ticket_options integer[]
NOT NULL DEFAULT ARRAY[1, 2, 3, 5, 10];
